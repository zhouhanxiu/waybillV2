# 万能导入 V4 —— 大模型调用说明

## 1. 概述

本系统在"运单智能导入"场景中集成了大模型，用于**自动分析用户上传的出库单文件（Excel/PDF），智能生成解析规则**，使系统能够从各种非标准格式的出库单中提取运单数据，大幅降低人工配置规则的成本。

> **V4 角色说明**：大模型仅辅助规则生成，不参与导入主链路（异步队列处理、批量校验、批量写入）。导入主链路在规则确认后完全离线执行，AI 不可用不影响导入吞吐。

### 核心能力

- 自动识别文件结构（标准行表格 / 卡片式 / 矩阵转置）
- 自动映射字段（配送单号、门店、收件人、SKU编码、数量等）
- 识别合并单元格、复合拆分、尾部收件人信息等复杂格式
- AI 分析结果供用户确认，不会自动执行

---

## 2. 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 大模型 | **智谱 GLM-4.5 Air** (`glm-4.5-air`) | 通过 OpenAI 兼容协议调用 |
| API 端点 | `https://open.bigmodel.cn/api/paas/v4` | 智谱 BigModel 平台 |
| AI SDK | **Vercel AI SDK v4** (`ai` 包) | 提供 `generateObject` 结构化输出 |
| Provider | `@ai-sdk/openai` | 基于 OpenAI 兼容协议 |
| Schema | **Zod** | 定义输出结构，强制执行格式校验 |
| 运行时 | Next.js 16 Edge / Node.js | 服务端调用，无浏览器依赖 |

### 为什么选择 GLM-4.5 Air

- 国内访问速度快，延迟低
- 中文理解能力强，适合物流出库单场景
- OpenAI 兼容 API，无缝集成 Vercel AI SDK
- 成本低，免费额度充足

---

## 3. 架构设计

```
┌──────────────────────────────────────────────────────────────┐
│                        前端 (page.tsx)                        │
│  用户上传文件 → 点击"AI 智能分析" → 进度条反馈 → 展示结果       │
└───────────────────────────┬──────────────────────────────────┘
                            │ POST /api/analyze (FormData)
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                   后端 API (route.ts)                         │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐    │
│  │              三层降级策略                               │    │
│  │  1️⃣ 已知模板匹配 (matchKnownFileRule) — 本地指纹       │    │
│  │  2️⃣ AI 大模型分析 (analyzeFileAndGenerateRule) — 远程  │    │
│  │  3️⃣ 本地兜底规则 (generateLocalRule) — 纯关键字映射     │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                               │
│  文件读取 → 文本预览生成 → 规则匹配/AI分析 → 返回规则JSON      │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                 大模型调用层 (ai.ts)                           │
│  prompt 工程 → Zod Schema → generateObject → 结构化输出       │
│  temperature=0.2 | maxRetries=1 | maxTokens 默认               │
└──────────────────────────────────────────────────────────────┘

                    ⚠️ AI 仅用于规则生成
                    
┌──────────────────────────────────────────────────────────────┐
│            V4 导入主链路（不依赖 AI）                          │
│  用户确认规则 → 上传文件 → 异步队列 → Worker 批量处理          │
│  （解析、校验、写入全部离线执行，AI 不可用不影响导入）          │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. 触发时机与流程

### 触发条件

用户上传出库单文件后，在界面上**手动点击"AI 智能分析"按钮**。AI 分析不会自动触发，需要用户主动操作。

### 完整交互流程

| 步骤 | 进度 | 操作 | 说明 |
|------|------|------|------|
| 1 | 10% | 正在上传文件... | 前端构建 FormData，将文件 POST 到 `/api/analyze` |
| 2 | 30% | AI 正在分析文件结构... | 服务端读取文件前 50 行，生成文本预览，调用 AI |
| 3 | 80% | 正在生成解析规则... | AI 返回结果，服务端整理为 ParseRule 结构 |
| 4 | 100% | 分析完成 | 前端展示 AI 生成的规则，用户确认或修改 |

### 用户确认环节

AI 分析完成后，前端展示：
- **规则名称**（可编辑）
- **文件类型**（Excel / PDF）
- **解析引擎**（行表格 / 卡片拆分 / 矩阵转置）
- **AI 推测字段列表**（`guessed`，标注为 AI 推测，供用户确认）
- **JSON 配置预览**
- 提供两个操作按钮：
  - **"试解析预览"**：用当前规则试跑，展示解析结果
  - **"保存规则并解析"**：保存规则到数据库并执行正式解析

---

## 5. Prompt 工程设计

### 5.1 系统角色

```text
你是一位物流出库单解析专家。请分析用户上传的文件样本，生成一个通用的解析规则配置。
```

### 5.2 目标字段定义（10 个字段）

| 字段 | 内部名 | 是否必填 | 说明 |
|------|--------|----------|------|
| 外部编码/配送单号 | `external_code` | 选填 | 用于聚合分组，必须是真正的配送单号 |
| 收货门店 | `store_name` | A组条件必填 | 收货门店/客户名称，非发货仓库 |
| 收件人姓名 | `receiver_name` | B组条件必填 | — |
| 收件人电话 | `receiver_phone` | B组条件必填 | — |
| 收件人地址 | `receiver_address` | B组条件必填 | — |
| SKU编码 | `sku_code` | 必填 | 物品编码 |
| SKU名称 | `sku_name` | 必填 | 物品名称 |
| 数量 | `quantity` | 必填 | 正数 |
| 规格型号 | `spec` | 选填 | — |
| 备注 | `remark` | 选填 | — |

> **收货信息规则**：A 组（`store_name`）与 B 组（`receiver_name` + `receiver_phone` + `receiver_address`）二选一必填。门店单用 A 组，快递单用 B 组。

### 5.3 三种解析引擎

| 引擎 | 适用场景 | 说明 |
|------|----------|------|
| `row` | 标准行表格 / 配送单 | 每行一条运单记录，最常见的格式 |
| `card` | 卡片式记录 / 调拨单 | 一块区域为一条记录，字段分散在区域内 |
| `matrix` | 矩阵转置 / 库存分货表 | 行头为 SKU，列头为门店，需行列转置 |

### 5.4 输入限制

- 输入样本为文件前 **50 行**的文本预览
- Token 限制：截取前 **4000 字符**
- 超过 4000 字符的样本会被自动截断

---

## 6. 调用参数

```typescript
const { object } = await generateObject({
  model,              // glm-4.5-air
  schema,             // Zod Schema
  prompt,             // Prompt 文本
  temperature: 0.2,   // 低温度，保证输出稳定、确定性高
  maxRetries: 1,      // 失败后重试 1 次
});
```

- **temperature=0.2**：规则生成需要确定性，低温度减少 AI 幻觉
- **maxRetries=1**：初次失败后自动重试一次，总调用次数最多 2 次

---

## 7. 三层降级策略

| 层级 | 方法 | 说明 |
|------|------|------|
| 第一层 | 已知文件模板匹配 | 纯本地，基于文件指纹匹配预设模板 |
| 第二层 | AI 大模型分析 | 远程调用，主要路径 |
| 第三层 | 本地关键字兜底 | AI 不可用时，基于表头关键字自动映射 |

---

## 8. V4 角色：AI 不参与导入主链路

V4 考试的核心是**异步事件驱动导入链路的性能**，大模型仅辅助规则生成：

- AI 在 `/api/analyze` 中生成解析规则 → 用户确认保存
- 用户使用已保存的规则上传文件 → 进入异步队列处理
- 队列 Worker 复用规则引擎执行批量解析、校验、写入
- **AI 不可用不影响导入吞吐量**，压测使用已确认的规则，不调用 AI

---

## 9. 安全原则

- AI 生成的规则仅供用户确认，不会绕过用户直接执行
- 低置信度字段前端以 ⚠️ 黄色标记显示
- 敏感数据不发送给 AI（仅发送表头和结构信息）
- AI 服务不可用时，用户可手动创建规则，系统完全可用

---

## 10. 环境变量配置

```bash
# 智谱 BigModel OpenAI 兼容 API
OPENAI_API_KEY=<your-api-key>
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AI_MODEL=glm-4.5-air
```

---

## 11. 相关文件索引

| 文件 | 职责 |
|------|------|
| `src/lib/ai.ts` | 核心：大模型调用、Prompt、Schema、降级策略 |
| `src/app/api/analyze/route.ts` | API 路由：`POST /api/analyze` |
| `src/app/page.tsx` | 前端：AI 按钮、进度条、结果展示 |
| `src/lib/parser/reader.ts` | 文件读取：Excel/PDF → 文本预览 |
| `.env.example` | 环境变量模板 |
