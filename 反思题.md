# 反思题

> V4 异步事件驱动重构 —— 考试反思题（不计分）

---

## 1. 为什么 V2 下单导入链路不能继续采用同步阻塞方式？什么时候同步反而更简单可靠？

**回答要点**：

**不能继续同步阻塞的原因**：
- Vercel Serverless 单次请求超时限制（约 10-30s），10,000 行数据同步解析 + 校验 + 写入耗时远超此限制
- 同步逐行 INSERT 数据库连接池会被瞬间打满，多个用户同时上传会导致系统雪崩
- 用户无法感知进度，只能看到 loading，体验极差
- 失败定位困难，日志里只有 "insert failed"，无法追溯到具体行号

**同步更简单可靠的场景**：
- 小文件（≤ 100 行）：同步链路简单，无需引入队列、Outbox 等额外复杂度
- 实时性要求极高的场景：比如用户上传后必须立即看到结果
- 原型验证阶段：快速验证业务逻辑，不需要过早优化架构

**核心判断标准**：处理时间是否超出请求超时限制。如果预估处理时间 < 超时阈值，同步反而是更简单的选择。

---

## 2. 处理单元变大或变小，会分别带来什么影响？

**回答要点**：

**处理单元变大（如 2000 行/单元）**：
- **优点**：减少队列消息数量，减少 Dispatch 和入队开销；减少数据库事务次数
- **缺点**：
  - 单个 Worker 执行时间变长，可能接近 Serverless 超时限制
  - 失败重试成本高，一个单元失败需要重试大量行
  - 批量查询 SKU 的 IN 子句可能过大，导致 SQL 性能下降
  - 并发度降低（单元数量少，并行度受限）

**处理单元变小（如 200 行/单元）**：
- **优点**：失败重试成本低；并发度高，可同时处理多个单元；单个 Worker 执行快
- **缺点**：
  - 队列消息数量暴增，Dispatch 开销增大
  - 数据库事务次数增加，连接池压力上升
  - 批量查询优势减弱（小批量接近逐行查询）

**本系统选择 1000 行/单元**：在并发度（10 个并行 Worker）和单 Worker 执行时间（~3-5s）之间取得平衡，是经过压测验证的合理选择。

---

## 3. 如果吞吐目标从 10,000 单/分钟提升到 50,000 单/分钟，最先成为瓶颈的是 Worker、Redis 队列还是数据库写入？你会如何扩展？

**回答要点**：

**最先瓶颈：数据库写入**。

原因：
- 50,000 行/分钟 ≈ 833 行/秒，批量 UPSERT 每次写 1000 行，需要 ~50 个批次
- PostgreSQL 连接池上限通常为 20-50，多 Worker 并发写入会迅速耗尽连接
- 磁盘 I/O 是物理限制，无法通过增加 Worker 无限扩展

**扩展方案**：
1. **数据库层面**：
   - 使用 `COPY` 协议替代批量 UPSERT（写入速度提升 5-10x）
   - 读写分离：主库写入，从库查询进度
   - 临时表合并：先写临时表，再批量 MERGE 到主表
   - 连接池优化：PgBouncer 事务池模式
2. **Worker 层面**：
   - 从 Serverless Worker 迁移到常驻进程（Railway/Render/Fly.io）
   - 动态批量大小：根据当前连接池使用率调整
3. **队列层面**：
   - QStash 本身吞吐量足够（Upstash 基础设施），暂不成为瓶颈
4. **Redis 层面**：
   - 当前仅用于幂等锁和进度缓存，不是瓶颈
   - 如需更高并发，可使用 Redis Cluster

---

## 4. 如果队列消息重复投递，为什么"业务幂等"比"消息只投递一次"更重要？

**回答要点**：

**"消息只投递一次"在分布式系统中几乎不可能保证**：
- 网络分区、Worker 崩溃、超时重试都会导致重复投递
- QStash 提供的是 at-least-once 语义，这是业界共识
- 追求 exactly-once 需要分布式事务（如两阶段提交），代价极高且降低吞吐

**"业务幂等"是更实际的解法**：
- 通过 task_id + unit_id 去重，重复消费时快速返回已完成结果
- 通过乐观锁（版本号）保证 processed_rows 不会重复累计
- 通过 UPSERT 基于稳定业务键（external_order_no + sku_code + line_no）避免重复写入

**结论**：依赖业务幂等比依赖消息中间件保证 exactly-once 更可靠、更简单、更高效。

---

## 5. 如果某个处理单元中有部分行失败，你认为应该整体回滚还是成功行先入库？为什么？

**回答要点**：

**应该成功行先入库，失败行记录错误明细**。

原因：
- **业务价值最大化**：10,000 行中 9,900 行成功、100 行失败，整体回滚意味着丢失 9,900 行有效数据，业务损失巨大
- **用户修复成本降低**：用户只需要修复 100 行失败数据，而不是重新上传 10,000 行
- **幂等性支持**：用户修复失败行后重新上传，已入库的成功行通过 UPSERT 去重，不会重复写入
- **状态清晰**：任务状态为 `partial_success`，用户可以明确知道哪些行成功、哪些行需要修复

**极端情况处理**：
- 如果整批 1000 行全部失败（如文件格式完全错误），批次标记为 `failed`，不影响其他批次
- 如果所有批次都失败，任务状态为 `failed`，用户需要重新上传

---

## 6. 错误明细中需要保留原始值，但手机号和地址属于敏感信息，你如何平衡排障效率和数据安全？

**回答要点**：

**脱敏策略**：
- **手机号**：保留前 3 位和后 4 位，中间 4 位替换为 `****`，如 `138****1234`
- **地址**：保留省市区，详细地址截断，如 `广东省深圳市南山区****`
- **姓名**：保留姓，名替换为 `*`，如 `张**`
- **非敏感字段**（SKU编码、数量、外部编码）：完整保留原始值

**实现方式**：
- 在 `processUnit.ts` 写入 `import_task_errors` 时，根据 `field_name` 判断是否需要脱敏
- 脱敏逻辑集中管理，不散落在各处
- 前端展示脱敏后的值，同时标注"已脱敏"

**平衡原则**：
- 排障需要：保留足够的特征信息（手机号前3后4、地址省市）让运维能判断是格式问题还是数据问题
- 数据安全：不暴露完整的个人信息，满足合规要求

---

## 7. 如果 Outbox 表持续增长到千万级，你会如何清理、归档和设计索引？

**回答要点**：

**索引设计**：
- 当前已有 `(status, next_retry_at)` 复合索引，满足 Dispatcher 轮询需求
- 可追加 `(created_at)` 索引用于按时间范围清理

**清理策略**：
1. **已完成事件**（status='sent'）：7 天后归档到冷存储（S3/对象存储），从主表删除
2. **失败事件**（status='failed', retry_count >= max）：30 天后归档删除
3. **pending 事件**（超过 24 小时未投递）：告警 + 人工处理

**归档方案**：
- 定时任务（Vercel Cron 每日执行）：`DELETE FROM event_outbox WHERE status='sent' AND created_at < NOW() - INTERVAL '7 days'`
- 归档前先导出到 CSV 或对象存储（可选）
- 使用分区表（按 `created_at` 月份分区），清理时直接 DROP 旧分区

**容量估算**：
- 每次导入 10,000 行创建 10 个 Outbox 事件（1000 行/单元）
- 日均 100 次导入 → 1,000 事件/天 → 365,000 事件/年
- 按 7 天保留 → 约 7,000 事件，远不到千万级
- 千万级场景需要引入分区表和更激进的清理策略

---

## 8. 如果 AI 生成的解析规则导致大量字段映射错误，监控系统如何帮助快速发现是规则问题而不是数据库问题？

**回答要点**：

**通过阶段耗时分布定位**：
- 如果**规则引擎阶段耗时异常**（P99 远高于正常值），说明规则本身有问题（如复杂正则匹配、字段映射错误导致大量类型转换失败）
- 如果**校验阶段耗时正常但错误率飙升**，说明规则映射的字段值不匹配 SKU 主数据，是规则问题而非数据库问题
- 如果**写入阶段耗时异常**，且错误码为 `E007`（数据库写入失败），才可能是数据库问题

**通过错误码分布定位**：
- 大量 `E006`（规则映射失败）→ 规则问题
- 大量 `E001`（SKU 不存在）→ 可能是规则映射了错误的 SKU 列
- 大量 `E002`（必填字段缺失）→ 规则可能遗漏了必填字段映射
- 大量 `E007`（数据库写入失败）→ 数据库问题

**监控看板快速判断**：
1. 打开监控看板 → 阶段耗时分布 → 看规则阶段 P99 是否异常
2. 打开错误类型分布 → 看 `E006` / `E001` 占比是否飙升
3. 打开 Trace 搜索 → 按 task_id 查看具体批次的错误明细

**与 AI 规则关联**：
- 任务记录中保存 `rule_id`，可以追溯到是哪个 AI 规则导致的问题
- 如果同一规则多次导致高错误率，可以标记该规则为"不可靠"并通知用户
